local based = {}

local AcceptId = {
	330071420,
	840658376
}
local GoldId = {
	330071420,
	840658376
}

function CheckPlayer(id)
	for v,i in pairs(AcceptId) do
		if i == id then
			checked = true
			print("checked")
			return true
		end
	end
end
function CheckGold(id)
	for v,i in pairs(GoldId) do
		if i == id then
			checked = true
			print("checked")
			return true
		end
	end
end
